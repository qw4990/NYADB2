/*
   multi_insert 检测NYADB在多线程下的插入执行, 以及崩溃后的恢复.

   具体的过程为:
        设置了40个不会结束的线程, 他们持续不断的向DB中插入数据, 这40个线程将并发
        然后设置了5个顺序执行的线程, 他们也会想DB中插入数据.
        接着在任意时刻, 强行关闭数据库, 接着重启数据库, 查看数据库内只包含已经提交的数据.

        40个线程的数据由multi_insert.go生成.
        5个顺序执行的线程的数据由multi_insert_commit.go生成.

        生成数据后, 执行start.sh.
        start.sh会先启动40个线程, 在后台执行, 接着就是执行那5个顺序执行的线程了.

    整个过程需要一定的手动操作.
*/
package main
